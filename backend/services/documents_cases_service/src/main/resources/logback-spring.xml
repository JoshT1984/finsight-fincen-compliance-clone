<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

  <!-- Console (Eclipse/IDE) -->
  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{HH:mm:ss.SSS} %highlight(%-5level) %cyan(%logger{20}) - %msg%n</pattern>
      <charset>UTF-8</charset>
    </encoder>
  </appender>

  <!-- File: persists so you can read logs even when Eclipse switches consoles -->
  <property name="LOG_PATH" value="${LOG_PATH:-logs}"/>
  <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>${LOG_PATH}/documents-cases-service.log</file>
    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
      <fileNamePattern>${LOG_PATH}/documents-cases-service.%d{yyyy-MM-dd}.log</fileNamePattern>
      <maxHistory>7</maxHistory>
    </rollingPolicy>
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger{36} - %msg%n</pattern>
      <charset>UTF-8</charset>
    </encoder>
  </appender>

  <!-- Audit / Security file -->
    <appender name="DOCUMENT_FILE"
              class="ch.qos.logback.core.rolling.RollingFileAppender">

        <file>/var/log/audit/documents_cases.log</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>/var/log/audit/documents_cases.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>

        <encoder>
            <!-- JSON only, no Logback metadata -->
            <pattern>%msg%n</pattern>
        </encoder>
    </appender>

    <!-- Only your audit logger writes to file -->
    <logger name="DOCUMENT_EVENT" level="INFO" additivity="false">
        <appender-ref ref="DOCUMENT_FILE"/>
    </logger>

  <root level="INFO">
    <appender-ref ref="CONSOLE"/>
    <appender-ref ref="FILE"/>
  </root>
  <logger name="com.skillstorm.finsight.documents_cases" level="DEBUG"/>
</configuration>
